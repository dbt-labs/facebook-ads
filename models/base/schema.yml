version: 2
models:
- name: fb_ad_creatives
  columns:
  - name: id
    tests:
    - not_null
    - unique
- name: fb_ad_creatives__child_links
  columns:
  - name: id
    tests:
    - relationships:
        field: id
        to: ref('fb_ad_creatives')
- name: fb_ads
  columns:
  - name: creative_id
    tests:
    - not_null
    - relationships:
        field: id
        to: ref('fb_ad_creatives')
  - name: id
    tests:
    - not_null
- name: fb_ad_insights
  columns:
  - name: ad_id
    tests:
    - not_null
  - name: campaign_id
    tests:
    - not_null
    - relationships:
        field: id
        to: ref('fb_campaigns')
- name: fb_campaigns
  columns:
  - name: id
    tests:
    - not_null
    - unique
